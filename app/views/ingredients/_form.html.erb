<%= form_with(model: @ingredient, local: true) do |form| %>
  <% if @ingredient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ingredient.errors.count, "error") %> prohibited this ingredient from being saved:</h2>

      <ul>
      <% @ingredient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field col-6" style="display: inline">
      <%= form.text_field :iname, class: 'form-control', placeholder: "Add new ingredient here..." %>
  </div>

  <div class="actions" style="display: inline">
    <%= form.submit "Save", class: 'btn btn-primary'%>

  </div>
<% end %>
